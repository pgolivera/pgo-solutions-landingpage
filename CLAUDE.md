Read README.md for full project context.

CSS is mobile-first: base styles target mobile, scale up with min-width media queries — never use max-width as the primary breakpoint strategy.

Commit message format: [PGO-#] - one line description of the change